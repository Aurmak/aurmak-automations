# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.*
!.env.example

# Graph mail config — holds a client secret, never committed. Start from
# graph-config.example.php and upload with scripts/upload-mail-config.sh.
graph-config.local.php
graph-config.*.local.php

# Local-only email preview output (scripts/preview-emails.sh)
preview-*.html

# Local stand-in for the server's Hestia private/ directory (api.php reads
# ../private/graph_config.php relative to public/). Test-only, never deployed —
# the deploy mirrors public/ alone.
/private/
